# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The Transformation Engine correctly executes orthogonal routing, but all four diagonal movements (`UP_LEFT`, `UP_RIGHT`, `DOWN_RIGHT`, `DOWN_LEFT`) suffer from a state-entry bug. When executed from the Base State ($F = 0$), the first diagonal command erroneously flips the geometric polarity to $F = 1$, causing the output to mirror an orthogonal inversion (e.g., `UP_RIGHT` yielding a minor chord like `RIGHT`, instead of translating to a major chord). Subsequent presses work because Matrix 2 correctly handles translations. The objective is to audit and correct the $F_{new}$ values for all diagonals in Matrix 1.

## 2. Technical Decisions & Dependencies
* **Reference Matrix 1 ($F = 0$) Diagonals:** All diagonal movements from the Base State are pure translations (Operation A) and MUST keep the state at $0$.
  * `UP_RIGHT` MUST equal: $\Delta r = +4$, $F_{new} = 0$
  * `DOWN_RIGHT` MUST equal: $\Delta r = +3$, $F_{new} = 0$
  * `DOWN_LEFT` MUST equal: $\Delta r = +8$, $F_{new} = 0$
  * `UP_LEFT` MUST equal: $\Delta r = +9$, $F_{new} = 0$

## 3. Implementation Phases

### Phase 1: Matrix 1 Diagonal Polarity Audit — ✅ Completed
* **Objective:** Locate the transcription errors in `Matrix 1` and ensure diagonals do not flip the $F$ state.
* **Instructions:**
    1. Open the file containing the transition matrices (e.g., `useTonnetzTransform.ts`).
    2. Audit the `Matrix 1` mapping object or `switch` statement (when `F === 0`).
    3. Overwrite the `F_new` (or polarity) values for `UP_RIGHT`, `DOWN_RIGHT`, `DOWN_LEFT`, and `UP_LEFT` to strictly equal `0`.
    4. Do not alter the $\Delta r$ values, as they are currently correct.
* **TDD Checkpoint:** * **Test Case 1:** [Given Engine state $r=0, F=0$ (C Major), When internal direction `UP_RIGHT` is triggered, Assert $r_{new}=4, F_{new}=0$ (E Major)].
    * **Test Case 2:** [Given Engine state $r=0, F=0$ (C Major), When internal direction `UP_LEFT` is triggered, Assert $r_{new}=9, F_{new}=0$ (A Major)].
    * **Status Update:** "Transition Phase status to 🔵 Active. Execute code audit and run tests. Update status to ✅ Completed before proceeding."

## 4. Final Review & Validation
* **Technical Audit:** Confirm Matrix 2 ($F = 1$) diagonals remain set to $F_{new} = 1$. Only Matrix 1 requires this polarity correction.
* **Status Reconciliation:** Verify all sub-phases match ✅ or ❌ before updating top-level status to ✅.
* **Final Sign-off:** Run a complete cycle for `UP_RIGHT` from C Major to verify it outputs E Major $\rightarrow$ G# Major $\rightarrow$ C Major without flipping to minor shapes. Present this result to the user.