]

# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives

The application requires a deterministic engine to handle 9th, 11th, and 13th extensions based on your predefined Chromatic Slide and LUM cycle transformations. The engine must transition from a binary `isFlipped` state to an explicit state machine tracking `currentRoot` and `isFlipped` for each chord quality, mapping directly to your provided shift values.

## 2. Technical Decisions & Dependencies

* **Engine Constraint:** The logic must strictly follow the provided matrices for 7th, 9th, 11th, and 13th chords.
* **State Machine:** The engine must handle two distinct states:
* `isFlipped = false` (Base Quality: e.g., ∆9, ∆9#11, ∆13#11)
* `isFlipped = true` (Flipped Quality: e.g., m9, m11, m13)


* **Modulo Math:** All root calculations must be `mod 12`.

## 3. Implementation Phases

### Phase 1: Direct Logic Wire-up — 🔘 Pending

* **Objective:** Implement the mapping of the D-Pad to your exact transformation rules.
* **Instructions:**
1. Update `handleDirectionalTrigger` in `useTonnetzTransform.ts` to implement the matrices provided for the extended chords.
2. Ensure that for every directional input, the system checks the `currentRoot` and `isFlipped` status and performs the exact addition/toggle defined in your specification.
3. **Hard Requirement:** Do not add "smart" quality-matching logic. The state must transition exactly as you defined for each chord extension (9th, 11th, 13th).


* **TDD Checkpoint:** * **Test Case 1:** [Given Maj9 (Root 0, isFlipped false), When `UP` triggered, Assert Root 1, isFlipped true (Min9)].
* **Test Case 2:** [Given Maj9 (Root 0, isFlipped false), When `RIGHT` triggered, Assert Root 4, isFlipped true (Min9)].
* **Status Update:** "Transition Phase status to 🔵 Active. Author and run tests. Update status to ✅ Completed before proceeding."



### Phase 2: Interval Mapping Enforcement — 🔘 Pending

* **Objective:** Ensure the visualizer renders the correct intervals based on the chord type.
* **Instructions:**
1. Implement a lookup for interval dictionaries:
* **9th Base:** `[0, 4, 7, 11, 2]`
* **9th Flipped:** `[0, 3, 7, 10, 2]`
* (Continue for 11ths and 13ths as defined in the spec).


2. `getOutputNotes` must now use these dictionaries to render the 5, 6, or 7 nodes correctly.


* **TDD Checkpoint:** * **Test Case 1:** [Given quality '∆9', When `getOutputNotes` executes, Assert the output is a 5-note array with the correct intervals].
* **Status Update:** "Transition Phase status to 🔵 Active. Author and run tests. Update status to ✅ Completed before proceeding."



## 4. Final Review & Validation

* **Technical Audit:** Verify that the D-Pad math matches your defined `Up = Chromatic Slide` and `Right = LUM` rules exactly for 9th, 11th, and 13th chords.
* **Status Reconciliation:** Verify all sub-phases match ✅ or ❌ before updating top-level status to ✅.
* **Final Sign-off:** Confirm sequence accuracy for 13th chords. Present the engine to you for manual verification.

