# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The Transformation Engine implemented in the previous phase has a critical routing bug. While `Matrix 1` ($F = 0$) executes correctly, `Matrix 2` ($F = 1$) is misrouting directional commands. Specifically, pressing "Left" while in the flipped state executes the logic for "Up" ($\Delta r = +0, F_{new} = 0$), resulting in parallel major/minor toggles instead of the correct Tonnetz geometric translations. The objective is to audit and correct the routing table/switch statements.

## 2. Technical Decisions & Dependencies
* **Strict Lookup Validation:** The error lies entirely within how the directional inputs map to the $\Delta r$ and $F_{new}$ values when $F_{current} == 1$.
* **Reference Matrix 2:** * `UP`: $\Delta r = +0$, $F_{new} = 0$
  * `LEFT`: $\Delta r = +8$, $F_{new} = 0$

## 3. Implementation Phases

### Phase 1: Matrix Routing Audit & Correction — ✅ Completed
* **Objective:** Locate the transcription error in the state transition logic and fix the mapping.
* **Instructions:**
    1. Open the file containing the `handleDirectionalTrigger` logic (e.g., `useTonnetzTransform.ts` or `App.tsx`).
    2. Audit the object dictionary or `switch` statement handling `Matrix 2` (when `F === 1`).
    3. Ensure `LEFT` maps exactly to `root shift: +8` and `new state: 0`.
    4. Verify all other Matrix 2 directions against the original spec: 
       * Up: +0, 0 | Up-Right: +4, 1 | Right: +3, 0 | Down-Right: +3, 1 
       * Down: +11, 0 | Down-Left: +8, 1 | Left: +8, 0 | Up-Left: +9, 1
    5. If a `switch` statement is used, ensure every case ends with a `break;` to prevent fall-through execution.
* **TDD Checkpoint:** * **Test Case 1:** [Given Engine state $r=9, F=1$ (A Minor), When direction `LEFT` is triggered, Assert $r_{new}=5, F_{new}=0$ (F Major)].
    * **Test Case 2:** [Given Engine state $r=9, F=1$ (A Minor), When direction `UP` is triggered, Assert $r_{new}=9, F_{new}=0$ (A Major)].
    * **Status Update:** "Transition Phase status to 🔵 Active. Execute code audit and run tests. Update status to ✅ Completed before proceeding."

## 4. Final Review & Validation
* **Technical Audit:** Confirm no other Matrix 1 or Matrix 2 directions share duplicate/incorrect values due to copy-paste errors.
* **Status Reconciliation:** Verify all sub-phases match ✅ or ❌ before updating top-level status to ✅.