# Product Requirements Prompt (PRP) — Strict Y-Axis Translation ✅

## 1. Project Context & Objectives
All standard CSS margin and positioning strategies have been swallowed by the DOM's transform scaling matrix. The Navigation widget remains stuck to the bottom of the screen. The absolute final objective is to force the UI upward by directly manipulating the Y-axis transform coordinates, bypassing the document flow entirely.

## 2. Technical Decisions & Dependencies
* **Target File:** Main View (`App.tsx` or `TonnetzGridContainer.tsx`).
* **Strategy:** Keep the element anchored at `bottom-0`, but inject `-translate-y-6` (moves the element exactly 24px up the Y-axis) directly into the transform stack alongside the scale modifier.

## 3. Implementation Phases

### Phase 1: GPU Transform Override — ✅ Completed
* **Objective:** Physically lift the component 24 pixels up using translation.
* **Instructions:**
  1. Open the file rendering `<NavContainer />` (e.g., `App.tsx`).
  2. Locate its absolute wrapper `div`.
  3. Replace its entire `className` string with exactly this:
     `"fixed bottom-0 left-1/2 z-[60] origin-bottom scale-[0.85] pointer-events-auto transform -translate-x-1/2 -translate-y-6"`
  4. *Crucial:* Ensure `mb-6` is removed from `NavControllerOriginal.tsx` if it was added in the previous step, so we don't have conflicting layout rules.
* **TDD Checkpoint:** * **Test Case 1:** [Given the `translate-y` utility is applied, When the app renders, Assert the GPU has physically lifted the widget 24px off the bottom of the screen.]
    * **Status Update:** "Transition Phase status to 🔵 Active. Execute the exact string replacement. Once verified, update status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Inspect the element in the browser dev tools to confirm the `transform` matrix includes `translateY(-1.5rem)`.
* **Status Reconciliation:** Verify all sub-phases match ✅ before updating top-level status to ✅.
* **Final Sign-off:** "[USER VERIFICATION REQUIRED] Please confirm the widget is finally, actually, physically off the bottom edge of the screen."