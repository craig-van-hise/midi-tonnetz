# Product Requirements Prompt (PRP) — Strict Margin Extraction & Execution ✅

## 1. Project Context & Objectives
The AI agent previously failed to correctly calculate and apply the bottom margin, resulting in the Navigation card sitting flush against the bottom of the screen (0 margin). The strict objective of this phase is to stop guessing, mathematically extract the exact spacing integer used by the existing bottom-left and bottom-right cards, and apply it to an unbreakable absolute wrapper for the Navigation widget.

## 2. Technical Decisions & Dependencies
* **Target File:** Main View (`App.tsx` or `TonnetzGridContainer.tsx`).
* **Strategy:** The agent MUST physically read the layout code governing the sibling cards. If the side cards are wrapped in a container with a padding class (e.g., `p-6`, `p-8`, `pb-6`), or have a direct margin/bottom class (e.g., `mb-8`, `bottom-8`), the agent will extract that exact integer (`6`, `8`, etc.) and apply it to the Nav card's `bottom-*` utility.

## 3. Implementation Phases

### Phase 1: Margin Math Extraction & Implementation — ✅ Completed
* **Objective:** Read the exact margin/padding math governing the side cards and apply it to the Nav widget.
* **Instructions:**
  1. Open `App.tsx` and `TonnetzGridContainer.tsx`.
  2. Locate the JSX for the side cards (the "Scroll to Pan" card on the bottom-left and the "Grid Axis" card on the bottom-right).
  3. Trace their parent DOM elements to identify how they are spaced from the bottom edge of the browser. Find the specific Tailwind spacing integer being used (for example, if they are inside a `<div className="absolute inset-0 p-6">`, the integer is `6`. If they have `<div className="absolute bottom-8">`, the integer is `8`).
  4. Define that extracted integer as `Y`.
  5. Locate the wrapper for `<NavContainer />`.
  6. Replace its `className` entirely with the following string, substituting `Y` for the exact integer you extracted:
     `"fixed bottom-4 left-1/2 -translate-x-1/2 z-[60] origin-bottom scale-[0.85] pointer-events-auto"`
* **TDD Checkpoint:** * **Test Case 1:** [Given the math is extracted and applied, When the app renders, Assert the bottom margin of the Nav card mathematically matches the side cards.]
    * **Status Update:** "Transition Phase status to 🔵 Active. Extract the integer, apply the exact class string, and verify. Once verified, update status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Double-check that the `bottom-Y` class applied to the Nav wrapper perfectly mirrors the spacing utility found in the side cards' layout. Ensure no nested elements inside `NavControllerOriginal` are artificially pulling the card down.
* **Status Reconciliation:** Verify all sub-phases match ✅ before updating top-level status to ✅.
* **Final Sign-off:** "[USER VERIFICATION REQUIRED] Please confirm the Nav card now has the exact same mathematical margin as the adjacent cards."