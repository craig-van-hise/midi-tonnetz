# Product Requirements Prompt (PRP) — Strict UI Overrides ✅

## 1. Project Context & Objectives
The previous phase failed to implement the layout adjustments. The Navigation widget is still sitting too high, and the "Learn Mode" popup is still anchored to the bottom of the screen, occluding the UI. The AI agent must immediately execute strict, explicit code overrides for these two specific UI components. DO NOT attempt to dynamically calculate or guess the layout; use the exact strings provided below.

## 2. Technical Decisions & Dependencies
* **Target Files:**
  * `components/navigation/NavContainer.tsx` (For the Learn Mode Banner).
  * Main View (`App.tsx` or `TonnetzGridContainer.tsx` where `<NavContainer />` is rendered) (For the widget wrapper margin).
* **Strategy:** Use explicit string replacement to force the Tailwind utilities. We will push the Learn Mode banner to `top-8` and forcefully anchor the Nav widget using `bottom-6` combined with `origin-bottom`.

## 3. Implementation Phases

### Phase 1: Hardcoded Learn Mode Banner Repositioning — ✅ Completed
* **Objective:** Move the "Learn Mode Active" overlay to the top of the screen using explicit string replacement.
* **Instructions:**
  1. Open `NavContainer.tsx`.
  2. Locate the following exact string in the JSX:
     `className="fixed bottom-8 left-1/2 -translate-x-1/2 bg-black text-white px-6 py-3 border-2 border-yellow-400 shadow-lg z-50 animate-bounce"`
  3. Replace `bottom-8` with `top-8` so the string becomes exactly:
     `className="fixed top-8 left-1/2 -translate-x-1/2 bg-black text-white px-6 py-3 border-2 border-yellow-400 shadow-lg z-50 animate-bounce"`
* **TDD Checkpoint:** * **Test Case 1:** [Given the code is updated, When the user triggers Learn Mode, Assert the banner appears strictly at the top of the viewport.]
    * **Status Update:** "Transition Phase status to 🔵 Active. Execute the exact string replacement. Once verified, update status to ✅ Completed."

### Phase 2: Brute-Force Margin Override — ✅ Completed
* **Objective:** Force the bottom margin of the Navigation widget to align with the standard UI spacing.
* **Instructions:**
  1. Locate the file where `<NavContainer />` is instantiated (e.g., `App.tsx` or `TonnetzGridContainer.tsx`).
  2. Find the wrapper `div` that surrounds `<NavContainer />`.
  3. Replace the entire `className` string of that specific wrapper `div` with exactly this:
     `"fixed bottom-6 left-1/2 -translate-x-1/2 z-50 origin-bottom scale-[0.85]"`
  4. *Note to Agent:* Do not use `bottom-1/4`, `bottom-10`, or try to calculate a generic offset. You must use `bottom-6` to match the standard `24px` spacing of the other cards, and you must include `origin-bottom` to prevent the `scale` transform from pulling the element upward.
* **TDD Checkpoint:** * **Test Case 1:** [Given the exact CSS class string is applied, When the app is rendered, Assert the Navigation widget is anchored tightly to the bottom of the screen.]
    * **Status Update:** "Transition Phase status to 🔵 Active. Execute the exact string replacement. Once verified, update status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Verify that the file changes strictly reflect the exact hardcoded Tailwind classes provided in the instructions.
* **Status Reconciliation:** Verify all sub-phases match ✅ before updating top-level status to ✅.
* **Final Sign-off:** "[USER VERIFICATION REQUIRED] Please confirm the Learn Banner is now at the top and the Navigation widget's bottom margin is finally corrected."